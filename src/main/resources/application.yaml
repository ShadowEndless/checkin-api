spring:
  application:
    name: checkin.api
#  autoconfigure:
#    exclude:
#      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
#  config:
#    import:
#      - optional:file:.env[ .properties]

#  datasource:
#    url: jdbc:postgresql://localhost:5432/api_checkin
#    username: postgres
#    password: **** ********************
  datasource:
    url: jdbc:postgresql://localhost:${DB_PORT}/api_checkin
    username: ${DB_USER}
    password: ${DB_PASS}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true>
